# CLAUDE.md

> SDD 开发脚手架 - Claude Code 专用配置
> 本文件由 SDD 脚手架自动生成

---

## 项目信息

- **项目类型**: SDD 规范驱动开发项目
- **脚手架版本**: v1.0
- **最后更新**: {自动填充}

## 开发规范

### 核心原则

1. **SPEC 是唯一真相源** - 所有需求必须来自 SPEC.md
2. **先 SPEC，后代码** - 任何修改前先确认 SPEC 中是否已定义
3. **验收标准驱动** - 实现必须满足 SPEC 中的验收标准
4. **变更必须更新 SPEC** - 代码变更前先更新 SPEC

### 工作流程

```
需求 → SPEC编写 → 开发实现 → 自测验证 → 验收确认
```

---

## 项目结构

```
docs/                           # SDD 脚手架文档
├── 01-使用说明.md              # 快速开始
├── 02-SDD方法论.md            # 核心概念
├── 03-工作流程.md              # 开发流程
├── 04-AI工具指南.md            # 本工具使用
├── 05-最佳实践.md              # 常见问题
├── 06-场景指南.md              # 三种开发场景
├── 07-小程序指南.md            # 小程序专项
├── 08-项目诊断器.md            # 自动诊断
├── templates/                  # 模板文件
│   ├── SPEC模板.md
│   ├── 变更日志模板.md
│   └── 代码审查清单.md
├── prompts/                    # AI 提示词
│   ├── 场景模式.md
│   ├── 从SPEC生成计划.md
│   └── 代码审查.md
├── skills/                     # 可复用 skill
├── agents/                     # agent 定义
└── scripts/                    # 自动化脚本
    ├── 初始化项目.ps1
    ├── 验证SPEC.ps1
    └── ...
```

---

## 开发场景

### 场景 A：有完整 PRD

```
> 请基于 docs/prd.md 生成 SPEC.md
```

### 场景 B：只有模糊想法

```
> 帮我做个 XXX，先做个简单的看看
```

### 场景 C：已有项目

```
> 请诊断当前项目
```

---

## 常用命令

```bash
# 初始化新项目
.\docs\scripts\初始化项目.ps1 -ProjectName "my-app"

# 验证 SPEC 完整性
.\docs\scripts\验证SPEC.ps1

# 诊断项目
> 请诊断当前项目
```

---

## AI 行为规范

### 必须做

- [ ] 开始前阅读 `docs/01-使用说明.md`
- [ ] 开发前阅读 `SPEC.md`
- [ ] 每次修改前对照验收标准
- [ ] 发现 SPEC 与代码不一致时提醒

### 禁止做

- [ ] 不添加 SPEC 外未明确的功能
- [ ] 不忽略验收标准
- [ ] 不跳过 SPEC 文档直接写代码

---

## 扩展能力

### Skills

查看 `docs/skills/` 目录获取可复用的 skill。

### Agents

查看 `docs/agents/` 目录获取预定义的 agent。

---

## 提示

- 使用 `/help` 查看 Claude Code 帮助
- 使用 `/session` 保存和恢复会话
- 使用 `/compact` 压缩上下文

---

*本文档由 SDD 脚手架自动生成*
