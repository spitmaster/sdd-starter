# .cursorrules

> SDD 开发脚手架 - Cursor 专用配置
> 本文件由 SDD 脚手架自动生成

---

## 项目类型

SDD 规范驱动开发项目

---

## 开发规范

### 核心原则

1. **SPEC 是唯一真相源**
2. **先 SPEC，后代码**
3. **验收标准驱动**
4. **变更必须更新 SPEC**

### 工作流程

```
需求 → SPEC编写 → 开发实现 → 自测验证 → 验收确认
```

---

## 项目结构

```
docs/
├── 01-使用说明.md              # 快速开始
├── 03-工作流程.md              # 开发流程
├── 06-场景指南.md              # 三种开发场景
├── templates/SPEC模板.md       # SPEC 模板
└── scripts/                    # 脚本
```

---

## Cursor 快捷指令

- `/rules` - 查看当前项目的 AI 行为规范
- `/spec` - 快速查看 SPEC.md 内容
- `/validate` - 运行 SPEC 验证脚本
- `/init-sdd` - 初始化一个新的 SDD 项目结构

---

## 开发场景

| 场景 | 说明 |
|------|------|
| A | 有完整 PRD → `> 请基于 docs/prd.md 生成 SPEC.md` |
| B | 模糊想法 → `> 帮我做个 XXX，先做个简单的` |
| C | 已有项目 → `> 请诊断当前项目` |

---

## 验收标准检查

每次代码修改后自动检查：

- [ ] 代码符合 SPEC.md 规格
- [ ] 满足验收标准
- [ ] 未添加未定义的功能
- [ ] SPEC 已更新（如有变更）

---

## 扩展

- Skills: `docs/skills/`
- Agents: `docs/agents/`

---

*本文档由 SDD 脚手架自动生成*
